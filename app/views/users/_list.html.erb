<!--<p id="notice">
	<%=  notice %>
</p>-->

<table class="table">
	<thead>
		<h3>User info</h3>
		<div>
			<%= attachment_image_tag user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %>
		</div>
	</thead>
	<tbody>
		<tr><th>name</th><td><%= user.name %></td></tr>
		<tr><th>introduction</th><td><%= user.introduction %></td></tr>
	</tbody>
</table>
			<%= link_to edit_user_path(user), class: "btn btn-default btn-block glyphicon glyphicon-wrench" do %>
			<% end %>


<h3>New book</h3>
<%= form_for(book) do |f| %>
<% if book.errors.any? %>
<div id="error_explanation" class="alert alert-danger">
	<h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>
	<ul>
		<% book.errors.full_messages.each do |message| %>
			<li><%= message %></li>
		<% end %>
	</ul>
</div>
<% end %>

 <h5>Title</h5>
  <%= f.text_field :title, class: "btn-block" %>
 <h5>Opinion</h5>
  <%= f.text_area :body, class: "btn-block" %><br>
  <%= f.submit 'Create Book' %>
<% end %>